# TODO

- [ ] Edit `index.html`: add additional tab(s) inside `.tabs-wrap` using existing `.tab` classes (no changes to current tabs).
- [x] Edit `index.html`: add matching new `.section` blocks with unique `id`s for the new tabs.
- [x] Populate new sections with large “JavaScript fundamentals” educational content (core values/types/functions/scope/closures + async/event loop + DOM + prototypes/classes).

- [ ] Keep styling consistent: same structural components (`section-header`, `intro-box`, `code-section`, `code-block`, `tip`, `warn`) and same code block markup.
- [ ] Do not modify `styles.css` or `app.js` (ensure existing show/tabs behavior still works).
- [ ] Run a quick check by opening `index.html` in browser and clicking the new tabs.

